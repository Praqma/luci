isWebsiteUp(){
  local host=$1
  local port=$2
  local line=$(runZettaTools curl -s --head $host:$port | head -n 1)
  echo "isWebSiteUp output : $line"
  echo $line | grep -q "HTTP/1.1 200 OK"
  #runZettaTools curl -s --head $host:$port | head -n 1 | grep -q "HTTP/1.1 200 OK"
}
